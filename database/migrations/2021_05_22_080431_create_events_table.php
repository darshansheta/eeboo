<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateEventsTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('events', function (Blueprint $table) {
            $table->id();
            $table->string('name');
            $table->unsignedInteger('max_participation')->nullable();
            $table->unsignedInteger('max_slot')->nullable();
            $table->unsignedInteger('max_slot_per_booking')->nullable();
            $table->unsignedInteger('duration');
            $table->unsignedInteger('preparation');
            $table->unsignedInteger('advance_booking_days');
            $table->date('start_date')->nullable();
            $table->date('end_date')->nullable();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('events');
    }
}
